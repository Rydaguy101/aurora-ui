{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "fade-in",
  "type": "registry:ui",
  "title": "Fade In",
  "description": "Scroll-triggered fade and blur entrance wrapper for any content.",
  "categories": [
    "Typography & Counters",
    "scroll",
    "reveal"
  ],
  "dependencies": [],
  "registryDependencies": [
    "utils"
  ],
  "files": [
    {
      "path": "components/ui/fade-in.tsx",
      "type": "registry:ui",
      "content": "\"use client\";\r\n\r\nimport React, { useRef } from \"react\";\r\nimport { motion, useInView } from \"framer-motion\";\r\nimport { cn } from \"@/lib/utils\";\r\n\r\ninterface FadeInProps {\r\n  children: React.ReactNode;\r\n  className?: string;\r\n  delay?: number;\r\n  duration?: number;\r\n  direction?: \"up\" | \"down\" | \"left\" | \"right\" | \"none\";\r\n  blur?: boolean;\r\n}\r\n\r\nconst offsets = {\r\n  up: { x: 0, y: 24 },\r\n  down: { x: 0, y: -24 },\r\n  left: { x: 24, y: 0 },\r\n  right: { x: -24, y: 0 },\r\n  none: { x: 0, y: 0 },\r\n};\r\n\r\nexport function FadeIn({\r\n  children,\r\n  className,\r\n  delay = 0,\r\n  duration = 0.6,\r\n  direction = \"up\",\r\n  blur = true,\r\n}: FadeInProps) {\r\n  const ref = useRef<HTMLDivElement>(null);\r\n  const isInView = useInView(ref, { once: true, margin: \"-10%\" });\r\n  const offset = offsets[direction];\r\n\r\n  return (\r\n    <motion.div\r\n      ref={ref}\r\n      initial={{\r\n        opacity: 0,\r\n        x: offset.x,\r\n        y: offset.y,\r\n        filter: blur ? \"blur(8px)\" : \"blur(0px)\",\r\n      }}\r\n      animate={\r\n        isInView\r\n          ? { opacity: 1, x: 0, y: 0, filter: \"blur(0px)\" }\r\n          : {}\r\n      }\r\n      transition={{ duration, delay, ease: [0.22, 1, 0.36, 1] }}\r\n      className={className}\r\n    >\r\n      {children}\r\n    </motion.div>\r\n  );\r\n}\r\n"
    }
  ],
  "docs": "# Fade In\nScroll-triggered fade and blur entrance wrapper for any content.\n## Import\n```tsx\nimport { FadeIn } from \"@/components/ui/fade-in\";\n```\n## Usage\n```tsx\n\"use client\";\n\n\n<FadeIn direction=\"up\" blur delay={0} />\n```\n## Props\n- **direction** (string) — default: `\"up\"`: Direction\n- **blur** (boolean) — default: `true`: Blur\n- **delay** (number) — default: `0`: Delay (s)\n> Requires `\"use client\"` in the file that renders this component.\nSome Aurora UI components use custom Tailwind animations (animate-shimmer, animate-marquee, animate-aurora, etc.). Merge keyframes from https://raw.githubusercontent.com/Rydaguy101/aurora-ui/main/tailwind.config.ts into your tailwind.config.\n## Install\n```bash\nnpx --yes github:Rydaguy101/aurora-ui add fade-in\n# or with shadcn CLI (after aurora-ui init):\nnpx shadcn@latest add @aurora/fade-in\n```",
  "meta": {
    "exportName": "FadeIn",
    "exportNames": [
      "FadeIn"
    ],
    "isClientComponent": true,
    "usageExample": "<FadeIn direction=\"up\" blur delay={0} />",
    "importExample": "import { FadeIn } from \"@/components/ui/fade-in\";",
    "props": [
      {
        "name": "direction",
        "type": "string",
        "default": "up",
        "description": "Direction"
      },
      {
        "name": "blur",
        "type": "boolean",
        "default": true,
        "description": "Blur"
      },
      {
        "name": "delay",
        "type": "number",
        "default": 0,
        "description": "Delay (s)"
      }
    ],
    "previewUrl": "https://aurora-ui-tau.vercel.app/components#fade-in",
    "docsUrl": "https://aurora-ui-tau.vercel.app/api/components/fade-in",
    "sourceUrl": "https://aurora-ui-tau.vercel.app/api/component-source/fade-in"
  }
}
